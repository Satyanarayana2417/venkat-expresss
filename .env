# Google Maps API Key (Optional - will fallback to free services if not provided)
# Get your API key from: https://console.cloud.google.com/google/maps-apis
VITE_GOOGLE_MAPS_API_KEY=

# Note: The application will work without the Google Maps API key
# It will use free reverse geocoding services as fallback
